# Flask Configuration
SECRET_KEY=your-secret-key-change-this-in-production
FLASK_ENV=production
DEBUG=True

# Database Configuration
DATABASE_URL=sqlite:///anipreneur.db

# Admin Access Configuration
# IP-based access control has been removed
# Admin panel is now accessible from any IP address

# Optional: Custom port (default is 5050)
# PORT=5050

# Optional: Custom host (default is 0.0.0.0)
# HOST=0.0.0.0 